name: リリースの自動作成
on:
  push:
    branches:
      - "release/**"
  pull_request:
    branches:
      - main
    types: [closed, opened]
jobs:
  release:
    name: release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@master
      - name: Return release version
        id: release_version
        env:
          REF: ${{ github.event.pull_request.ref }}
        run: | 
          VERSION=${REF##*/}
          echo "::set-output name=version_number::$VERSION"
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.release_version.outputs.version_number }}
          release_name: ${{ steps.release_version.outputs.version_number }}
          body: ${{ github.event.pull_request.body }}
          draft: false
          prerelease: false